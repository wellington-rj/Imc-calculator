import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxTitle:{
        alignItems:"center",
        justifyContent:"center",
        padding:10,
     },

     textTitle:{
        color:"rgba(219, 32, 101, 1)",
        fontSize:24,
        fontWeight:"bold",
     }
});

export default styles